{
    "$id": "https://meta.comcast.com/firebolt/localization",
    "title": "Localization",
    "oneOf": [
        {
            "$ref": "#/definitions/CountryCode"
        },
        {
            "$ref": "#/definitions/ISO639_2Language"
        },
        {
            "$ref": "#/definitions/Locale"
        }
    ],
    "definitions": {
        "CountryCode": {
            "type": "string",
            "pattern": "^[A-Z]{2}$"
        },
        "ISO639_2Language": {
            "type": "string",
            "pattern": "^[a-z]{3}$"
        },
        "Locale": {
            "type": "string",
            "pattern": "^[a-zA-Z]+([a-zA-Z0-9\\-]*)$"
        }
    }
}