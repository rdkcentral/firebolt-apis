---
mode: agent
description: Add a new Firebolt API. Runs a plain-English interview to determine the module, operation, Smithy trait, and types — no Smithy knowledge required. Creates an OpenSpec change and implements it end-to-end.
---

Use the `new-fb-api` skill to add a new Firebolt API.

${input:description:Describe the API you want to add in plain English — e.g. "a read-only property on the Display module that returns the current brightness as a percentage"}
