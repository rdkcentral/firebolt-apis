name: Standalone

on:
  push:
    branches-ignore: [ main ]
  pull_request:
    types:
      - opened
      - synchronize
    branches: [ main ]

jobs:
  RunStandaloneTests:
    runs-on: ubuntu-latest
    steps:
      - name: Get an open-rpc from firebolt-apis
        run: |
          echo "HI"